# 发货系统接手规则

本仓库由 Codex、通过 Codex 接入的 DeepSeek 等共同维护。开始任何修改前，先读 Obsidian 真源 `E:\CODEX\1-项目\仓库系统管理\11-共享接手状态.md`、`07-更新日志.md`、本仓库 README，以及 `E:\CODEX\AGENTS.md`。本机仓库路径是 `E:\CODEX\1-项目\仓库系统管理\订单报表系统`；旧 `D:\订单报表` 和旧腾讯云笔记仅供历史参考。

## 修改前

1. 核对本机 Git、GitHub 远端、阿里云服务器 `/home/ubuntu/zy-shipping` 的实际状态；先完成共享状态页列出的未结对账。服务器 Git HEAD 不能单独代表运行代码，因为曾有未提交文件直接部署。
2. 修改生产数据库、照片或面单前，确认目标服务器与数据库并备份；不要从本机 SQLite 或旧 Supabase 反向覆盖生产。
3. 保留订单行绑定、发货批次、快递关联的可追溯性；有歧义的数据交给人工确认。

## 修改后

1. 代码完成时，在 Obsidian `07-更新日志.md` 追加开发记录：北京时间、需求、改动、提交号、测试、剩余问题；同时更新 `11-共享接手状态.md`。未部署时明确写“未上线”。
2. 生产部署后另写部署记录：实际运行提交及工作区、迁移与备份、健康检查、关键业务验收、回退点。缺少现场证据时写“未确认”。
3. 原则上先提交到仓库再部署。紧急直接改服务器时，必须在同一次工作把差异带回仓库、提交并补记部署信息；不要让服务器独留无提交代码。
4. 不把密码、私钥、令牌、数据库连接密钥或客户隐私写入 Git 或云同步知识库。

当前生产入口为 `www.zyfahuo.cn`（阿里云）。当前细节与待办以共享状态页为准，不能依赖聊天记忆。
